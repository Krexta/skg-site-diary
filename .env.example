# データベース情報
POSTGRES_HOST="db"
POSTGRES_USER="root"
POSTGRES_PORT="5432"
POSTGRES_PASSWORD="password"
POSTGRES_DATABASE="SampleDB"

# サーバーのポート番号
PORT=3000

DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DATABASE}?schema=public
DATABASE_URL_TEST=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DATABASE}?schema=test
